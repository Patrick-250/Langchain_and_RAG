RAG contains of retrieval,augmenting and generation...

a triad consists of  a query,a response and the context...
these comoponents in a triad all work together to improve the answer relevance.....

components of Rag: Retriever and generator...

Retriever identifies and retrieves relevant documents
Generator takes the retrieved docs and the input query to generate a coherent and contextuallu relevant response.

Rag is more of a(not really a framework lols)... that combines retrieval based systems and generation based models to produce a more accurate and relevant responses...


inject more knowlwedge to a LLM than what it was trained on by augmenting it...

Naive_Rag:
docs--->chunks--->embed with embedding model-->vectorize(like store the embeddings in a vectorDB)--->user querry-->embedd queery----> serach similar vectors in our DB-->Retrieve and show

naive RAG might have limited contexts since it would prolly retrieval broad docs...

we need to improve it......optimize it...


